#!/bin/bash

echo "
A Collection of Ballin Scripts!
https://github.com/JBallin/ballin-scripts

Commands:

    ballin_update         update to latest version (maintains config, re-runs init)
    ballin_config         ''/'get' (no args) view config
                          'get' KEY (ex: get theme.dark)
                          'set' KEY VALUE (ex: set up.cleanup true)
    ballin_uninstall      remove ballin-scripts (including symlinks)

Scripts:

    gu                    gist: '' (update) 'open', 'read [file_name]'
    theme                 'd' (dark), 'l' (light), '' (toggle)
                          'save' (save current theme to config): 'd' 'l'
    up                    update brew etc.
"
