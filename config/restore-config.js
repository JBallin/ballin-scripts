// const fs = require('fs');
// const savedConfig = process.argv[2];
// const savedConfigObj = JSON.parse(config);
// const defaultConfig = require('config');
//
// for (let key in defaultConfig) {
//   const savedValue = defaultConfig[key];
//   if (changeConfig[key])
// }
